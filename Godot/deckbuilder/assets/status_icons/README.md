# Status Icons

Drop status effect icons here using the C# status component type name.

Examples:

- `Weak.png`
- `Vulnerable.svg`
- `regen.webp`

The battle party UI checks this folder for `png`, `svg`, `webp`, `jpg`, and `jpeg` files. If no matching asset exists, it falls back to a single-character text marker.
